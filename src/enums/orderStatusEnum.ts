export enum orderStatusEnum {
	PENDING = "pending",
	ACCEPTED = "accepted",
	REJECTED = "rejected",
	OTW = "otw", // Stands for "On The Way"
	DELIVERED = "delivered",
	SERVED = "served",
	CANCELED = "canceled",
}
